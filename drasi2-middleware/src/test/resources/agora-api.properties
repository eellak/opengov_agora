baseUrl = ${test.baseUrl}

# Set by maven buildnumber plugin
build.number = ${buildNumber}
build.timestamp = ${timestamp}

client.ode.maxDecisionsRead = 100
client.ode.maxDecisionsWrite = 5

document.hashing.algorithm = SHA-256

mail.host = ${test.mail.host}
mail.from = ${test.mail.from}
mail.encoding = UTF-8
mail.prefix = ${test.mail.prefix}
# Maven UTF-8 filtering for .properties file seems broken, hardcode for now
mail.from.name = Σύστημα Αυτόματης Ειδοποίησης

db.diavgeia-ro.driver = ${test.db.diavgeia-ro.driver}
db.diavgeia-ro.url = ${test.db.diavgeia-ro.url}
db.diavgeia-ro.username = ${test.db.diavgeia-ro.username}
db.diavgeia-ro.password = ${test.db.diavgeia-ro.password}

db.diavgeia-rw.driver = ${test.db.diavgeia-rw.driver}
db.diavgeia-rw.url = ${test.db.diavgeia-rw.url}
db.diavgeia-rw.username = ${test.db.diavgeia-rw.username}
db.diavgeia-rw.password = ${test.db.diavgeia-rw.password}

db.agora.driver = ${test.db.agora.driver}
db.agora.url = ${test.db.agora.url}
db.agora.username = ${test.db.agora.username}
db.agora.password = ${test.db.agora.password}

db.agora-ro.driver = ${test.db.agora-ro.driver}
db.agora-ro.url = ${test.db.agora-ro.url}
db.agora-ro.username = ${test.db.agora-ro.username}
db.agora-ro.password = ${test.db.agora-ro.password}